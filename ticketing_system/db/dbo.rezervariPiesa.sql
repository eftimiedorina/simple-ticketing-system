CREATE TABLE [dbo].[rezervariPiesa] (
    [idPiesa] INT  NOT NULL,
    [rand]    INT  NOT NULL,
    [loc]     INT  NOT NULL,
    [nume]    TEXT NOT NULL,
    PRIMARY KEY CLUSTERED ([idPiesa] ASC)
);

